---
layout: page
title: yacut
permalink: /projects/yacut/
---

![YaCut]({{ '/assets/img/YaCut.png' | relative_url }})

## ✂️ Описание

**YaCut** — асинхронный сокращатель ссылок на **Flask**,
позволяющий создавать короткие URL и управлять ими через веб-интерфейс и API.
Дополнительно реализована интеграция с **Яндекс.Диском** для загрузки и скачивания файлов.

Проект выполнен в рамках обучения, но архитектура и асинхронная логика реализованы **самостоятельно**.

---

## 💡 Что реализовано

* Создание коротких ссылок (вручную или автоматически)
* Переадресация по короткому URL
* Асинхронная работа с API Яндекс.Диска (`aiohttp`)
* Веб-интерфейс с формой и flash-сообщениями
* API-эндпоинты с валидацией входных данных
* Кастомные страницы ошибок (404, 500)

---

## 🛠️ Технологии

* **Python 3.11**, **Flask (async)**
* **SQLAlchemy**, **WTForms**, **Jinja2**
* **Aiohttp**

---

## 🚀 Установка и запуск проекта

Клонируйте репозиторий:

```bash
git clone https://github.com/MaksZakharov/async-yacut.git
cd async-yacut
```

Создайте и активируйте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # для Linux/MacOS
venv\Scripts\activate     # для Windows
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

Запустите проект:

```bash
flask run
```

После запуска приложение будет доступно по адресу:
👉 [http://localhost:5000](http://localhost:5000)

---

## 🌐 Пример API

**Создать короткую ссылку**

```http
POST /api/id/
Content-Type: application/json

{
  "url": "https://example.com",
  "custom_id": "example"
}
```

**Ответ**

```json
{
  "short_link": "http://localhost:5000/example",
  "url": "https://example.com"
}
```

---

## 🧾 Пример работы с Яндекс.Диском

Загрузка файла:

```python
await yandex_upload_file(file, filename, disk_token)
```

Скачивание файла:

```python
await yandex_get_download_link(filename, disk_token)
```

---

## 👨‍💻 Автор проекта

**Maks Zakharov**

🔗 [GitHub-репозиторий](https://github.com/MaksZakharov/async-yacut)

---

## 📜 Лицензия

Проект создан в рамках обучения.
Можно использовать в образовательных и демонстрационных целях.
